/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
                    "media.licdn.com", 
                    "lh3.googleusercontent.com",
                    "localhost"
                ],
    }
};

export default nextConfig;
